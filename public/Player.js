class Player {
    constructor(player) {
      this.id = player.id;
      this.name = player.name;
      this.score = player.score;
      this.effects = player.effects;
      this.myturn = player.myturn;
    }

    // updateEffects() {
    
    // }
  
  }